# marketing_dashboard

Naver/Google 검색광고(또는 유사 채널)의 캠페인 성과 데이터를 분석하고, Streamlit으로 마케팅 대시보드를 만드는 프로젝트.

## 프로젝트 목표

- `raw/` 아래의 원본 CSV를 정제·가공하여 분석 가능한 형태로 만든다.
- 핵심 지표(노출수, 클릭수, CTR, CPC, 비용, 전환/구독, 전환율, 전환당비용)를 기준으로 캠페인 성과를 파악한다.
- Streamlit 대시보드에서 기간별 추이, 광고그룹/키워드별 성과, 기기·시간대별 성과를 인터랙티브하게 탐색할 수 있게 한다.

## 개발 환경

- Python 가상환경: `venv/` (Windows: `venv\Scripts\activate`, Git Bash: `source venv/Scripts/activate`)
- 의존성은 `requirements.txt`로 관리한다. 새 패키지 추가 시 `pip install <pkg>` 후 `pip freeze > requirements.txt`로 갱신.
- 대시보드 실행: `streamlit run app.py` (앱 파일명은 추후 확정)

## 데이터 구조 (`raw/`)

원본 데이터는 모두 한글 컬럼명의 CSV이며, 하나의 캠페인을 여러 각도로 쪼갠 파일들이다. 코드에서 컬럼명을 영문으로 리네이밍할 때는 일관된 매핑 테이블을 한 곳(`src/schema.py` 등)에 두고 재사용한다.

| 파일 | 내용 | 그레인(row 단위) |
|---|---|---|
| `campaign_daily.csv` | 일별 캠페인 성과 (날짜, 요일, 노출수, 클릭수, CTR, 평균CPC, 비용, 구독 신청, 전환율, 전환당비용, 예산 소진 여부, 학습 기간 여부) | 일자 |
| `ad_groups.csv` | 광고그룹별 요약 성과 (키워드 수, 평균 품질평가점수, 노출/클릭/CTR/CPC/비용/구독신청) | 광고그룹 |
| `keywords.csv` | 키워드별 성과 (품질평가점수, 광고 관련성, 예상 CTR, 방문 페이지 품질, 노출/클릭/CTR/CPC/비용/구독신청, 고객 아님) | 키워드 x 광고그룹 |
| `search_terms.csv` | 실제 검색어 성과 (관련도, 일치 키워드, 광고그룹, 검색유형, 노출/클릭/CTR/비용/구독신청/전환당비용) | 검색어 |
| `device_hour.csv` | 기기별·시간대별 성과 (`구분` 컬럼이 "기기" 또는 "시간대"로 두 種類 데이터가 세로로 합쳐져 있음 — 분석 전 `구분` 값으로 분리 필요) | 기기 or 시간대 |
| `placements.csv` | 게재위치별 성과 (현재 비어있음 — 데이터 채워지면 스키마는 다른 파일과 동일 패턴) | 게재위치 |

### 주의할 데이터 특이사항

- 숫자처럼 보이는 컬럼 중 `CTR`, `전환율`은 `%` 문자열(예: `2.16%`)로 저장되어 있어 로드 시 float 변환 필요.
- `평균 CPC`, `비용`, `전환당비용`은 원 단위 정수로 보이나 콤마 등 구분자 유무를 로드 시 확인할 것.
- `전환당비용`은 구독 신청이 0건인 행에서 값이 비어 있음(0으로 나누기 방지 — 결측 처리, 0으로 강제 변환 금지).
- `campaign_daily.csv`의 `예산 소진`, `학습 기간`은 Y/공백의 플래그 컬럼 — boolean으로 변환.
- `device_hour.csv`는 기기 데이터와 시간대 데이터가 한 파일에 union 되어 있으므로, `구분` 컬럼으로 먼저 `df[df['구분']=='기기']` / `df[df['구분']=='시간대']`로 분리 후 사용.
- `search_terms.csv`의 `관련도`(정확 일치/관련/느슨함/무관)는 검색어 품질을 나타내는 범주형 값 — 필터/색상 인코딩에 활용 가치가 큼.
- 파일 인코딩은 한글 포함 CSV이므로 로드 시 `encoding='utf-8-sig'` 우선 시도, 깨지면 `cp949` 확인.

## 분석/대시보드 설계 가이드

- 원본 CSV는 절대 직접 수정하지 않는다. 정제 로직은 `src/` 아래 별도 모듈에서 pandas로 처리하고, 필요하면 `processed/`에 중간 산출물을 저장한다(원본과 혼동되지 않게 디렉터리 분리).
- 지표 계산(CTR, CPC, 전환율, 전환당비용 등)은 원본에 이미 있는 컬럼을 신뢰하되, 파생 지표를 새로 만들 때는 원본 값과 재계산 값이 다를 경우 그 이유를 주석으로 남긴다.
- 대시보드 페이지 구성 제안:
  1. 개요: 기간 선택 + 핵심 KPI 카드(노출/클릭/CTR/비용/구독신청/전환율/전환당비용) + 일별 추이 차트
  2. 광고그룹/키워드 분석: 성과 랭킹 테이블, 품질평가점수 vs CTR/CPC 관계
  3. 검색어 분석: 관련도별 분포, 저성과 검색어(무관·느슨함인데 비용 높은 케이스) 하이라이트
  4. 기기/시간대 분석: 기기별 비교, 시간대별 히트맵 또는 라인 차트
- 차트 라이브러리는 Plotly 권장(인터랙티브 hover, Streamlit과 궁합 좋음).
- 캐싱: 데이터 로드/전처리 함수에 `@st.cache_data` 적용해 매 상호작용마다 CSV 재로드하지 않도록 한다.

## 코드 스타일

- 데이터 처리 함수는 순수 함수로 작성하고 Streamlit UI 코드(`st.*` 호출)와 분리한다 (`src/data.py` vs `app.py`).
- 컬럼명은 내부적으로 영문 snake_case로 통일하고, UI에 표시할 때만 한글 라벨로 매핑한다.
- 하드코딩된 파일 경로 대신 `pathlib.Path` 기반 상대 경로 사용.
